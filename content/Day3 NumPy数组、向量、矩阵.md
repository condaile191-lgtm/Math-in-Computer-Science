
# 历史

雅可比  哈密尔顿 凯莱 克莱姆-克莱姆法则； 高斯 欧拉

## 哈密顿
复数 a+bi 
平面：三维空间里的点？
有没有一个数可以表示三维旋转？
不是3个分量 而是4个分量
四元数：a+bi+cj+dk

四元数 `a+bi+cj+dk` 可用于表示三维旋转。

四元数的运用：3d游戏 角色旋转、计算机图形学、机器人、航天器。
“数不知是数，还可以是向量，是更复杂的代数结构”

## 凯莱

发明了矩阵。

矩阵由数表组成，可表达线性方程组和线性变换。

矩阵的本质是线性变换。相当于改变一个空间的基向量。

矩阵作用在向量上，本质上是在改变空间中的方向和尺度。

放缩：

放缩变换示例：对角矩阵可分别拉伸或压缩各坐标轴。

```
需要证明a<c则，找到一个中间量b，证明a<b且b<c即可。
```


夹逼思想（夹逼准则, 迫敛性）

夹逼思想：用上下界把目标量限制住，从而证明极限或不等式。

## 格拉斯曼
线性扩张论：定义了向量空间、线性无关、维数、内积、外积

向量空间是线性代数的核心概念

## 高斯
高斯函数 高斯消元  高斯分布 高斯函数，高斯智断瓶中线 求和公式，数学王子，哥廷根学派
1+2+3+...+100 怎么算？ 5050 高斯等差数列
画出正17边形 尺规作图

复数多项式 根的基础
算术研究 数论的奠基之作 “数论宪章” 二次互反律 一生给出6种证明
古神星 -> 最小二乘法 拟合观测数据
高斯曲率 微分几何的核心
非欧几何
地球磁场图
高斯函数又叫什么？取整函数 
高斯分布又叫正态分布
最小二乘法 线性回归
高斯消元法
消元思想

新工业时代数学有很大的作用。
代数的基本定理
同余，二次互反律 -> 强基计划、竞赛
国际生

# 最小二乘法
最小二乘法，用矩阵来解超定方程组。
让误差的平方和最小。


# 向量

向量既有大小又有方向，可表示物理量、特征和嵌入。

向量的源头：
向量，既有大小，又有方向

物理：速度、加速度、位移，都是向量。
信yibo，研究数学比研究言情小说要好。

几何：有向线段就是向量

计算机：特征、嵌入、权重，都是向量

# 特征向量

feature 特征向量：是事物的属性清单

如何描述玫瑰花？红色，有香味的花

```
**近景 / 特写景别的植物摄影。**  
画面主体是一簇盛开的白色小花，位于画面中央偏上位置，花蕊呈黄色，周围被大片浓绿色叶片包围。镜头距离主体较近，只截取了植物的一部分枝叶与花朵，没有展示完整植株或大范围环境。背景为树林或植被，经过明显虚化，形成绿色散景，使前景花朵更加突出。
```


**Feature（特征）**可以简单理解成：
> **用来描述一个事物的某个属性。**
比如描述一朵玫瑰，可以选这些特征：
$$
\text{颜色、香味、花瓣数量、花径、茎长}
$$
如果把它写成一串数：
$$
x= \begin{bmatrix} 1\\ 1\\ 32\\ 8.5\\ 45 \end{bmatrix}
$$
可以约定：

- 第1维：是否红色，红色 = 1
- 第2维：是否有香味，有 = 1
- 第3维：花瓣数 = 32
- 第4维：花径 = 8.5 cm
- 第5维：茎长 = 45 cm

# embedding 嵌入向量

embedding
把文字、图片 -> embed -> 稠密向量（代表文字、图片的向量，特征向量的升级版。）


# weight 权重向量/权重矩阵

权重说的是你的特征有多重要。
看房子，楼层、面积影响房价，权重不一样。

# 矩阵的源头

三个仓库，物品不同。
三个未知数，进行求解。

矩阵最直观的用途之一，就是把一组线性方程统一写成一个结构。

示例：
$$
\begin{cases}
x + 2y + 3z = 10 \\
4x + 5y + 6z = 20 \\
7x + 8y + 9z = 30
\end{cases}
$$
写成矩阵形式：
$$ \begin{bmatrix} 1 & 2 & 3 \\ 4 & 5 & 6 \\ 7 & 8 & 9 \end{bmatrix} \begin{bmatrix} x \\ y \\ z \end{bmatrix} = \begin{bmatrix} 10 \\ 20 \\ 30 \end{bmatrix} $$


为什么需要向量？
	word2vec
	一张图片、一个用户、一个词都是向量。
	向量让我们可以用数学来处理多维数据。

为什么需要矩阵？
	矩阵本质是线性变换。
	关系复杂
	矩阵让我们可以批量处理数据，并行运算

为什么需要numpy? 
	python原生列表速度慢 numpy c写的
	用了simd指令（Single Instruction, Multiple Data，单指令多数据流） 支持向量化运算 不需要写循环 就能对整个数组进行运算。
	cpu只下达一次命令，同时对许多组数据做同样的计算。
	举例：对列表中所有数字加一，本质for循环依次加一
	simd 只需要发一次指令，就全部加1了。

为什么Numpy默认cpu\不用gpu?
	cpu + simd  就像：一个老师傅8个杯子倒水。
	gpu 有很多小计算核心，启动开销大。就像：几千个工人流水线。

# 向量运算


```
# 向量加法：对应分量相加
[1,2,3] + [4,5,6] = [5,7,9]
```

```
# 向量数乘：每个分量都乘以这个数
2 * [1,2,3] = [2,4,6]
```

```
# 向量点积：对应分量相乘，再求和 
[1,2,3] . [4,5,6] = 1x4 + 2x5 + 3x6 = 32
```

向量点积的几何意义：
$$
a⋅b=∥a∥∥b∥cosθ
$$
衡量2个向量的相似度，点积越大，方向越近。

## 矩阵

NumPy 数组示例：一维数组形状通常为 `(3,)`。

二维矩阵示例：形状可写为 `(行数, 列数)`。

数组维度由 `ndim` 表示，形状由 `shape` 表示。

向量、矩阵和更高维张量都可由 NumPy `ndarray` 表示。

这里的 `(3,)` 不是说“3维”，而是：
> **第 1 个维度的长度是 3**
`ndim` 看有几个方向（轴），`shape` 看每个方向有多长。

核心区分：`ndim` 看轴的数量，`shape` 看每个轴的长度。

# 作业

作业：用 NumPy 创建数组并观察维度、形状和基本运算结果。

# numpy向量运算

NumPy 向量运算可直接对整个数组进行加法、数乘和点积。

# 范数

向量的模长称之为范数。

范数表示向量长度，常见为欧氏范数。

## 向量夹角公式

我们本来想计算两个向量在方向上有多一致，结果推导以后发现，在直角坐标系里，它恰好等于“对应坐标相乘再相加”。

点积公式：`a·b=||a||||b||cosθ`，用于衡量方向相似度。

点积越大，两个向量方向越接近；点积为 0 表示垂直。


垂直的向量，点积为0:
垂直向量的点积为 0。


单位向量：
单位向量长度为 1，可表示纯方向。

乘法 矩阵变换的几何意义
`A * B`：对应位置相乘
A @ B：真正的矩阵乘法

矩阵乘法表示线性变换的复合，`A @ B` 不等于逐元素相乘。

旋转矩阵可以把一个向量旋转，同时保持向量长度不变。

旋转矩阵可改变向量方向，同时保持长度不变。
列向量约定下，通常写作 `A @ x` 表示矩阵作用于向量。

行向量写法也可以使用，但需要保持约定一致。

**不是线性变换天生必须左乘。**
是因为数学里通常约定“向量写成列向量”，于是变换矩阵自然放左边。

矩阵放左侧主要来自列向量书写习惯。