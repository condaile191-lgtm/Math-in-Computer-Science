# Math in Computer Science

Obsidian notes published as a Quartz site on GitHub Pages.

- Source notes live at the repository root.
- `scripts/prepare-content.mjs` mirrors root Markdown notes and `assets/` into Quartz `content/` during build.
- GitHub Pages deploys through `.github/workflows/deploy.yml`.
