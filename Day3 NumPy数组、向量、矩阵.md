
# 历史

雅可比  哈密尔顿 凯莱 克莱姆-克莱姆法则； 高斯 欧拉

## 哈密顿
复数 a+bi 
平面：三维空间里的点？
有没有一个数可以表示三维旋转？
不是3个分量 而是4个分量
四元数：a+bi+cj+dk

图解要点：四元数把复数从二维扩展到四个分量 `a+bi+cj+dk`，常用于三维旋转。它比欧拉角更适合计算机图形学和机器人中的旋转计算。

四元数的运用：3d游戏 角色旋转、计算机图形学、机器人、航天器。
“数不知是数，还可以是向量，是更复杂的代数结构”

## 凯莱

发明了矩阵。

图解要点：矩阵最初可以看作数表，但更重要的是它能表示线性变换。凯莱等人把矩阵运算系统化，使矩阵成为线性代数核心工具。

矩阵的本质是线性变换。相当于改变一个空间的基向量。

图解要点：矩阵乘向量可以理解为“变换空间”。它可能放大、缩小、旋转或剪切向量，因此矩阵不只是表格，而是一个动作。

放缩：

图解要点：放缩矩阵通常是对角矩阵，例如 x 方向乘一个系数，y 方向乘另一个系数。它会改变长度，但不一定改变方向关系。

```
需要证明a<c则，找到一个中间量b，证明a<b且b<c即可。
```


夹逼思想（夹逼准则, 迫敛性）

夹逼准则：如果 `g(x)≤f(x)≤h(x)`，且当 `x→a` 时 `g(x)` 与 `h(x)` 都趋向同一个极限 L，那么 `f(x)` 也趋向 L。

## 格拉斯曼
线性扩张论：定义了向量空间、线性无关、维数、内积、外积

向量空间是线性代数的核心概念

## 高斯
高斯函数 高斯消元  高斯分布 高斯函数，高斯智断瓶中线 求和公式，数学王子，哥廷根学派
1+2+3+...+100 怎么算？ 5050 高斯等差数列
画出正17边形 尺规作图

复数多项式 根的基础
算术研究 数论的奠基之作 “数论宪章” 二次互反律 一生给出6种证明
古神星 -> 最小二乘法 拟合观测数据
高斯曲率 微分几何的核心
非欧几何
地球磁场图
高斯函数又叫什么？取整函数 
高斯分布又叫正态分布
最小二乘法 线性回归
高斯消元法
消元思想

新工业时代数学有很大的作用。
代数的基本定理
同余，二次互反律 -> 强基计划、竞赛
国际生

# 最小二乘法
最小二乘法，用矩阵来解超定方程组。
让误差的平方和最小。


# 向量

图解要点：向量的源头来自有向量，如速度、位移、力；在计算机中，图片、文字、用户特征也可表示成向量，方便计算相似度和变换。

向量的源头：
向量，既有大小，又有方向

物理：速度、加速度、位移，都是向量。
信yibo，研究数学比研究言情小说要好。

几何：有向线段就是向量

计算机：特征、嵌入、权重，都是向量

# 特征向量

feature 特征向量：是事物的属性清单

如何描述玫瑰花？红色，有香味的花

```
**近景 / 特写景别的植物摄影。**  
画面主体是一簇盛开的白色小花，位于画面中央偏上位置，花蕊呈黄色，周围被大片浓绿色叶片包围。镜头距离主体较近，只截取了植物的一部分枝叶与花朵，没有展示完整植株或大范围环境。背景为树林或植被，经过明显虚化，形成绿色散景，使前景花朵更加突出。
```


**Feature（特征）**可以简单理解成：
> **用来描述一个事物的某个属性。**
比如描述一朵玫瑰，可以选这些特征：
$$
\text{颜色、香味、花瓣数量、花径、茎长}
$$
如果把它写成一串数：
$$
x= \begin{bmatrix} 1\\ 1\\ 32\\ 8.5\\ 45 \end{bmatrix}
$$
可以约定：

- 第1维：是否红色，红色 = 1
- 第2维：是否有香味，有 = 1
- 第3维：花瓣数 = 32
- 第4维：花径 = 8.5 cm
- 第5维：茎长 = 45 cm

# embedding 嵌入向量

embedding
把文字、图片 -> embed -> 稠密向量（代表文字、图片的向量，特征向量的升级版。）


# weight 权重向量/权重矩阵

权重说的是你的特征有多重要。
看房子，楼层、面积影响房价，权重不一样。

# 矩阵的源头

三个仓库，物品不同。
三个未知数，进行求解。

矩阵最直观的用途之一，就是把一组线性方程统一写成一个结构。

示例：
$$
\begin{cases}
x + 2y + 3z = 10 \\
4x + 5y + 6z = 20 \\
7x + 8y + 9z = 30
\end{cases}
$$
写成矩阵形式：
$$ \begin{bmatrix} 1 & 2 & 3 \\ 4 & 5 & 6 \\ 7 & 8 & 9 \end{bmatrix} \begin{bmatrix} x \\ y \\ z \end{bmatrix} = \begin{bmatrix} 10 \\ 20 \\ 30 \end{bmatrix} $$


为什么需要向量？
	word2vec
	一张图片、一个用户、一个词都是向量。
	向量让我们可以用数学来处理多维数据。

为什么需要矩阵？
	矩阵本质是线性变换。
	关系复杂
	矩阵让我们可以批量处理数据，并行运算

为什么需要numpy? 
	python原生列表速度慢 numpy c写的
	用了simd指令（Single Instruction, Multiple Data，单指令多数据流） 支持向量化运算 不需要写循环 就能对整个数组进行运算。
	cpu只下达一次命令，同时对许多组数据做同样的计算。
	举例：对列表中所有数字加一，本质for循环依次加一
	simd 只需要发一次指令，就全部加1了。

为什么Numpy默认cpu不用gpu?
	cpu + simd  就像：一个老师傅8个杯子倒水。
	gpu 有很多小计算核心，启动开销大。就像：几千个工人流水线。

# 向量运算


```
# 向量加法：对应分量相加
[1,2,3] + [4,5,6] = [5,7,9]
```

```
# 向量数乘：每个分量都乘以这个数
2 * [1,2,3] = [2,4,6]
```

```
# 向量点积：对应分量相乘，再求和 
[1,2,3] . [4,5,6] = 1x4 + 2x5 + 3x6 = 32
```

向量点积的几何意义：
$$
a⋅b=∥a∥∥b∥cosθ
$$
衡量2个向量的相似度，点积越大，方向越近。

## 矩阵

NumPy 示例：`v=np.array([1,2,3])` 是一维数组，`shape` 为 `(3,)`，表示只有一个轴，这个轴上有 3 个元素。

NumPy 示例：`A=np.array([[1,2,3],[4,5,6]])` 是二维矩阵，`shape` 为 `(2,3)`，表示 2 行 3 列。

NumPy 要点：`ndim` 表示有几个轴；`shape` 表示每个轴的长度。例如 `(2,3)` 是二维，第一轴长度 2，第二轴长度 3。

NumPy 要点：一维数组可看作向量，二维数组可看作矩阵，三维及以上数组可看作张量。统一结构都是 `ndarray`。

这里的 `(3,)` 不是说“3维”，而是：
> **第 1 个维度的长度是 3**
`ndim` 看有几个方向（轴），`shape` 看每个方向有多长。

核心区分：`ndim` 回答“有几个方向/轴”，`shape` 回答“每个方向有多长”。理解这两个属性是掌握 NumPy 数组的基础。

# 作业

作业提示：分别创建一维向量和二维矩阵，打印 `shape`、`ndim`，再比较列表循环与 NumPy 向量化计算的写法。

# numpy向量运算

NumPy 向量运算：`v1+v2` 做对应分量相加，`v1*2` 做数乘，`np.dot(v1,v2)` 做点积。它避免手写循环，表达更接近数学。

# 范数

向量的模长称之为范数。

范数表示向量长度。欧氏范数可用 `np.linalg.norm(v)` 计算，例如 `[1,2,3]` 的长度是 `sqrt(1²+2²+3²)`。

## 向量夹角公式

我们本来想计算两个向量在方向上有多一致，结果推导以后发现，在直角坐标系里，它恰好等于“对应坐标相乘再相加”。

点积公式：`cosθ=(v1·v2)/(|v1||v2|)`。它把代数计算和几何夹角联系起来，可用于衡量两个向量方向是否接近。

图解要点：点积为正通常表示夹角小于 90°，方向大致一致；点积为 0 表示垂直；点积为负表示方向相反成分更多。


垂直的向量，点积为0:
示例：`a=[1,0]`，`b=[0,1]` 时，`np.dot(a,b)=0`，说明两个坐标轴方向互相垂直。


单位向量：
单位向量：把向量除以自身范数即可归一化，例如 `unit_v=v/np.linalg.norm(v)`。单位向量保留方向，长度变为 1。

乘法 矩阵变换的几何意义
`A * B`：对应位置相乘
A @ B：真正的矩阵乘法

矩阵运算要点：`A*B` 是对应元素相乘；`A@B` 是真正的矩阵乘法，表示线性变换的组合或方程组运算。

旋转矩阵可以把一个向量旋转，同时保持向量长度不变。

旋转矩阵：二维旋转可写为 `[[cosθ,-sinθ],[sinθ,cosθ]]`。它会改变向量方向，但保持向量长度不变。
列向量约定：数学中常把向量写成列向量，所以线性变换通常写作 `A @ x`，表示矩阵 A 作用在向量 x 上。

行向量约定也可以使用，但乘法方向会变成 `x @ A` 或需要转置矩阵。关键不是谁“天生正确”，而是前后一致。

**不是线性变换天生必须左乘。**
是因为数学里通常约定“向量写成列向量”，于是变换矩阵自然放左边。

结论：矩阵常写在左边，主要因为默认向量是列向量；如果改用行向量，也可以把变换写在右边，但公式体系要整体调整。